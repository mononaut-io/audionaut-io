import { FuzzySearchPipe } from './fuzzy-search.pipe';

describe('FuzzySearchPipe', () => {
  it('create an instance', () => {
    const pipe = new FuzzySearchPipe();
    expect(pipe).toBeTruthy();
  });
});
